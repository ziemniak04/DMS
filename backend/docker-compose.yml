version: '3.8'

services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: dms-backend
    ports:
      - "8000:8000"
    environment:
      # Application Settings
      - APP_NAME=DMS Backend API
      - APP_VERSION=1.0.0
      - DEBUG=true

      # Dexcom API Configuration
      - DEXCOM_CLIENT_ID=${DEXCOM_CLIENT_ID}
      - DEXCOM_CLIENT_SECRET=${DEXCOM_CLIENT_SECRET}
      - DEXCOM_REDIRECT_URI=${DEXCOM_REDIRECT_URI:-http://localhost:8000/auth/callback}
      - DEXCOM_ENVIRONMENT=${DEXCOM_ENVIRONMENT:-sandbox}

      # Firebase Configuration
      - FIREBASE_PROJECT_ID=${FIREBASE_PROJECT_ID}
      - FIREBASE_CREDENTIALS_PATH=/app/firebase-credentials.json

      # Database Configuration
      - DATABASE_URL=${DATABASE_URL}

      # Encryption Key
      - ENCRYPTION_KEY=${ENCRYPTION_KEY}

      # CORS Origins
      - CORS_ORIGINS=["http://localhost:3000","http://localhost:8080"]

      # Rate Limiting
      - DEXCOM_RATE_LIMIT=60000
      - DEXCOM_RATE_WINDOW=3600

    volumes:
      # Mount firebase credentials if using local file
      - ./firebase-credentials.json:/app/firebase-credentials.json:ro
      # Mount code for development (hot reload)
      - ./app:/app/app

    restart: unless-stopped

    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/health')"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s

    networks:
      - dms-network

  # Optional: PostgreSQL database (if not using Firestore)
  # postgres:
  #   image: postgres:15-alpine
  #   container_name: dms-postgres
  #   environment:
  #     - POSTGRES_DB=dms
  #     - POSTGRES_USER=dms_user
  #     - POSTGRES_PASSWORD=dms_password
  #   ports:
  #     - "5432:5432"
  #   volumes:
  #     - postgres_data:/var/lib/postgresql/data
  #   networks:
  #     - dms-network
  #   restart: unless-stopped

  # Optional: Redis for distributed rate limiting
  # redis:
  #   image: redis:7-alpine
  #   container_name: dms-redis
  #   ports:
  #     - "6379:6379"
  #   volumes:
  #     - redis_data:/data
  #   networks:
  #     - dms-network
  #   restart: unless-stopped
  #   command: redis-server --appendonly yes

networks:
  dms-network:
    driver: bridge

# volumes:
#   postgres_data:
#   redis_data:
